const count = 8;
const rows = [];
for (let i = 0; i < count; i = i + 1) {

 i;
} 
// This code initializes an empty array called `rows` and fills it with numbers from 0 to `count - 1`.
for (let i = 0; i < count; i = i + 1) {
  rows.push(i);
}

for (const row of rows){
    result = result  + row + "\n";
}

for (let i = 0; i < count; i = i + 1) {
  rows.push(character)
}